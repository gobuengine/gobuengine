[{"name":"New","code":"local requireAll = {Transform}\n\n-- test phaser\nlocal phaser = {OnPhysics = 1, OnUpdate = 2, onDrawing = 4, OnDrawingUI = 6, OnStart = 7}\n\n\nEngine.System(phaser.OnStart, function(it)\n\tprint \"Start System\"\nend)\n\nEngine.System(phaser.OnUpdate, function(it)\n\t-- Obtenemos el componente\n\tlocal transform = Engine.get_component(it, 1, \"Transform\")\n\t-- Obtenemos la cantidad de entidades\n\tlocal entitys = Engine.get_entitys(it)\n\t-- Aplicamos por cada entidad\n\tfor i = 0, entitys, 1 do\n\t\t-- Obtenemos la rotation de la entidad x\n\t\tlocal rotation = Engine.get_rotate(transform, i)\n\t\t-- Aplicamos una nueva rotation a la entidad x\n\t\tEngine.set_rotate(transform, i, rotation + 0.5)\n\tend\nend)\n\n"},{"name":"WeaponCursorRotate","code":""},{"name":"MoveControllerPawn","code":""},{"name":"HealthComponent","code":""}]